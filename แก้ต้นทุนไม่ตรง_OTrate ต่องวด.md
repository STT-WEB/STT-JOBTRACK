# แก้ต้นทุนลงจ๊อบไม่ตรง BP — OT Rate ต่องวด

## ปัญหา (สรุป 1 บรรทัด)
สูตร **คอลัมน์ K (OT Rate)** ในแท็บ `JOB_COST_DIRECT` ดึงเรต **ตัวเดียวต่อคนทั้งเดือน** (จาก `PAYROLL_SUMMARY`)
แต่ **ค่าจ้างรายวันจริงเปลี่ยนต่องวด** (เช่น 1068016 = 400 งวดแรก / 440 งวดหลัง) → OT คิดผิด → ยอดไม่ตรง

## ต้นตอเชิงลึก
- Payroll จ่าย OT = ชั่วโมง × (อัตราจริงงวดนั้น ÷ 8) × ตัวคูณ
- แต่ engine ใช้ 52.50 ทุกบรรทัด (= ค่าเฉลี่ย/เรตนิ่งเก่า) → เพี้ยนทุกคนที่ค่าจ้างขยับ

## วิธีแก้ (แก้จุดเดียว — คอลัมน์ K)
เปลี่ยนสูตร `K3` ให้ดึง **อัตราจริงต่องวด** จาก `PAYROLL_ACTUAL` (จับคู่ด้วย รหัสพนักงาน + งวดที่คำนวณจากวันที่ทำงาน) แล้ว ÷ 8
เฉพาะ **พนักงานรายวัน** — รายเดือนคงสูตรเดิม (อัตราของรายเดือนไม่ใช่ค่าจ้างรายวัน)

```
=LET(
  d,$A3, emp,$B3, typ,$I3,
  pk, TEXT(IF(DAY(d)>=26,EOMONTH(d,0)+1,d),"yyyy-mm")&"-"&IF(DAY(d)>=26,"A",IF(DAY(d)>=11,"B","A")),
  cnt, COUNTIFS(PAYROLL_ACTUAL!$G:$G,emp, PAYROLL_ACTUAL!$BI:$BI,pk),
  IF( AND(ISNUMBER(SEARCH("รายวัน",typ)), cnt>0),
      SUMIFS(PAYROLL_ACTUAL!$J:$J, PAYROLL_ACTUAL!$G:$G,emp, PAYROLL_ACTUAL!$BI:$BI,pk)/8,
      IFERROR(XLOOKUP(emp,PAYROLL_SUMMARY!$A:$A,PAYROLL_SUMMARY!$I:$I),0) )
)
```

### กติกา "งวด" (จากวันที่ทำงาน A)
- วันที่ **26 เดือนก่อน – 10** → งวดแรก (รหัส `A`, จ่าย 15)
- วันที่ **11 – 25** → งวดหลัง (รหัส `B`, จ่าย 30)
- โค้ด PeriodKey ในไฟล์ = `2026-05-A` / `2026-05-B` (สูตรข้างบนสร้างให้ตรงอัตโนมัติ)

## ขั้นตอนทำ (ครั้งเดียว ใช้กับทุกคน)
1. เปิดแท็บ `JOB_COST_DIRECT`
2. คลิกช่อง **K3** → วางสูตรด้านบน → Enter
3. คลิก K3 อีกที → เลือก K3 ลงไปถึงแถวสุดท้ายที่มีข้อมูล → กด **Ctrl+D** (เติมสูตรลงทุกแถว)
4. ดูแท็บ `DATA_QUALITY_CHECK` — ช่อง "ต้นทุนลง Job ครบไหม?" ควรขึ้น ✅ เกือบทั้งหมด

> ทำครั้งเดียว = แก้ให้ **ทุกคน** พร้อมกัน ไม่ต้องไล่ทีละคน

## ผลทดสอบจริง (เดือน พ.ค. 2026, พนง.รายวัน 54 คน)
| | ผ่าน ≤1 บาท | ไม่ผ่าน | ยอดเพี้ยนรวม |
|---|---|---|---|
| เดิม (เรตตัวเดียว) | 46 | 8 | −828 บาท |
| **แก้แล้ว (เรตต่องวด)** | **53** | **1** | **−414 บาท** |

- 1068016: เดิมเกิน −3.65 → แก้แล้ว **0.06 บาท** ✅

## เหลือ 1 เคสให้ HR เช็ค (ไม่ใช่บั๊กเรต — เป็นข้อมูลชั่วโมงไม่ตรง)
**1068055** — Payroll จ่าย OT งวดหลัง (11–25 พ.ค.) = **41 ชม.** แต่ Timesheet มีแค่ **36.5 ชม.** → หาย 4.5 ชม. (~413 บาท)
→ ตรวจว่า Timesheet ขาดรายการ OT หรือ Payroll จ่ายเกิน นี่คือหน้าที่ของ reconcile (จับให้เจอ ไม่ซ่อน)

## หลักการถาวร (จำไว้ใช้ทุกเดือน)
- **ต้นทุนปกติ (ฐาน):** ใช้ยอดฐาน+สวัสดิการทั้งเดือน ÷ ชม.หาร — **ไม่ต้องแยกงวด** (ผลรวมตรง BP อยู่แล้ว)
- **ต้นทุน OT/วันหยุด (รายวัน):** ต้องใช้ **อัตราจริงต่องวด ÷ 8** เท่านั้น
- ส่วนต่าง ≤ 1 บาท = ผ่าน (เศษปัด) · > 1 บาท = ธงแดง หาสาเหตุ
